[project]
name = "pygame-ai-platformer"
version = "0.1.0"
description = "Pygame製マリオ風2Dプラットフォーマー + AI実験環境"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.5.0",
    "numpy>=1.26.0",
    "pandas>=2.2.0",
    "scikit-learn>=1.4.0",
    "joblib>=1.3.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[project.scripts]
play = "pygame_ai_platformer.main:main"
ai-demo = "pygame_ai_platformer.ai_demo:main"
collect-data = "pygame_ai_platformer.collect_data:main"
train-ml = "pygame_ai_platformer.train_ml_agent:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pygame_ai_platformer"]
